{{#with (lookup projects.pages key) as |project|}}
<article class="w-100 d-flex flex-col center">
	<section class="max-width page-p d-flex flex-col my-2 gap-1 center">
		<h1>{{project.heading}}</h1>
	</section>
</article>
{{/with}}
